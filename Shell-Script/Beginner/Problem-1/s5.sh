
#---------------------------------------- TASK 5 ---------------------------------------------

#Note:
#	The command will only work if the permissions are allowed.
#	You must first allow the necessary permissions to the script.
#	This can't be made generic as the root password are not known.

NOW=$'CURRENT CALENDAR\n'$(date);echo "$NOW" > /home/Planning.txt

echo "Here is my connection directory:" $HOME

echo $PATH > /home/"#SOURCESCOM"

cat /etc/passwd | sort > /home/FCMPT
